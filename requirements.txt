# PyTorch with CUDA 12.4 support (RTX 40 series)
torch==2.6.0
torchvision==0.21.0
torchaudio==2.6.0

# Hugging Face ecosystem
transformers==4.49.0
accelerate==1.4.0
huggingface_hub==0.28.1

# 4-bit quantization support (VRAM 절약)
bitsandbytes==0.45.3
